// import React from 'react';
// import './icons.svg';

const IconsImg = (props) => {
    console.log(props);

    {/*<svg className={`Icons Icons-${props.name}`}>*/}
        {/*<use xlinkHref={`#Icons_${props.name}`} />*/}
    {/*</svg>*/}

}

export default IconsImg;